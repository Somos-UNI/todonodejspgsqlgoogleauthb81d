name: Trigger auto deployment for container-app-urndq4zcq3hco

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-urndq4zcq3hco-AutoDeployTrigger-4ecf584f-84e2-441e-931d-0ae6ae673c4f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPURNDQ4ZCQ3HCO_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPURNDQ4ZCQ3HCO_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPURNDQ4ZCQ3HCO_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPURNDQ4ZCQ3HCO_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPURNDQ4ZCQ3HCO_REGISTRY_PASSWORD }}
          containerAppName: container-app-urndq4zcq3hco
          resourceGroup: todonodejspgsqlgoogleauthb81d8d0a
          imageToBuild: default/container-app-urndq4zcq3hco:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


